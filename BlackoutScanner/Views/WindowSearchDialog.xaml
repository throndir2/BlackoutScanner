<Window x:Class="BlackoutScanner.Views.WindowSearchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select Window"
        Height="400"
        Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select a window from the list of running applications:"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <Grid Grid.Row="1"
              Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="Filter:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <TextBox Grid.Column="1"
                     x:Name="filterTextBox"
                     TextChanged="FilterTextBox_TextChanged"/>
        </Grid>

        <DataGrid Grid.Row="2"
                  x:Name="windowsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  MouseDoubleClick="WindowsDataGrid_MouseDoubleClick"
                  Background="{DynamicResource ControlBackgroundBrush}"
                  Foreground="{DynamicResource ForegroundBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Window Title"
                                    Binding="{Binding Title}"
                                    Width="3*"/>
                <DataGridTextColumn Header="Process Name"
                                    Binding="{Binding ProcessName}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Select"
                    Click="Select_Click"
                    IsDefault="True"
                    Margin="5"
                    Padding="10,5"/>
            <Button Content="Cancel"
                    Click="Cancel_Click"
                    IsCancel="True"
                    Margin="5"
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>
